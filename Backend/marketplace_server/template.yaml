AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Local Marketplace Server (auto-generated)

Metadata:
  Note: |
    Functions discovered from dispatcher: 33
    - registerUser
    - loginUser
    - …

Resources:   # 👈 must be here, no extra spaces
  MarketplaceFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: MarketplaceFunction
      Runtime: python3.11
      Handler: lambda.lambda_handler
      CodeUri: ./
      Timeout: 30
      MemorySize: 512
      Events:
        Api:
          Type: Api
          Properties:
            Path: /{proxy+}
            Method: ANY
